# Client dependencies for running test scripts locally

# HTTP and REST clients
httpx==0.26.0           # Async HTTP client for REST API

# WebSocket client
websockets==12.0        # WebSocket protocol implementation

# SSE (Server-Sent Events) client
aiohttp==3.9.1          # Async HTTP with SSE support

# gRPC clients
grpcio==1.60.0          # gRPC runtime
grpcio-tools==1.60.0    # Protocol buffer compiler tools
protobuf==4.25.2        # Protocol buffer runtime

# Optional: For testing message persistence (Cassandra/Kafka)
# Uncomment if you want to test persistence layer directly
cassandra-driver==3.29.1  # Cassandra database driver
kafka-python==2.0.2       # Kafka Python client
redis==5.0.1              # Redis client (for monitoring)
