version: '3.8'

services:
 
   # MinIO Node 1 - Distributed mode with 5 nodes
  minio-1:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio-1
    ports:
      - "9000:9000"  # API port
      - "9001:9001"  # Console port
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server http://minio-{1...5}/data --console-address ":9001"
    volumes:
      - minio_data_1:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - microservices-net

  # MinIO Node 2
  minio-2:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio-2
    ports:
      - "9010:9000"
      - "9011:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server http://minio-{1...5}/data --console-address ":9001"
    volumes:
      - minio_data_2:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - microservices-net

  # MinIO Node 3
  minio-3:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio-3
    ports:
      - "9020:9000"
      - "9021:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server http://minio-{1...5}/data --console-address ":9001"
    volumes:
      - minio_data_3:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - microservices-net

  # MinIO Node 4
  minio-4:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio-4
    ports:
      - "9030:9000"
      - "9031:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server http://minio-{1...5}/data --console-address ":9001"
    volumes:
      - minio_data_4:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - microservices-net

  # MinIO Node 5
  minio-5:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: minio-5
    ports:
      - "9040:9000"
      - "9041:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server http://minio-{1...5}/data --console-address ":9001"
    volumes:
      - minio_data_5:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - microservices-net
